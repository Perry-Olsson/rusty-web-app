services:
  app:
    build:
      context: .
      target: dev
      args:
        APP_NAME: app
    image: app
    container_name: app
    user: "${USER_ID:-1000}:${GROUP_ID:-1000}"
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true
